{
  "name": "apexdigits",
  "version": "1.0.0",
  "description": "ApexDigits Portfolio & Cloud Architecture Consulting",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "mkdir -p dist && cp -r *.html assets styles.css script.js vercel.json robots.txt sitemap.xml dist/ 2>/dev/null || true",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
